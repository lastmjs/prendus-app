directive @userOwns(field: String) on FIELD | FIELD_DEFINITION

type User {
    id: ID! @unique
    createdAt: DateTime!
    updatedAt: DateTime!
    email: String! @unique @userOwns(field: "id")
    password: String!
}
