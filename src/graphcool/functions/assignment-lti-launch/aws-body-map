Content-Type: x-www-form-urlencoded

Template:

##  See http://docs.aws.amazon.com/apigateway/latest/developerguide/api-gateway-mapping-template-reference.html
##  This template will pass through all parameters including path, querystring, header, stage variables, and context through to the integration endpoint via the body/payload
#set($allParams = $input.params())
{
    "query": "mutation { assignmentLTILaunch(, assignmentId: \"$input.params('assignmentid')\", assignmentType: \"$input.params('assignmenttype')\", requestBody: \"$input.body\", path: \"$context.resourcePath\", method: \"$context.httpMethod\") { ltiJWT, ltiSessionIdJWT, clientRedirectUrl, serverRedirectUrl } }"
}
